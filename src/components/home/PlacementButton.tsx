type Props = {};

export const PlacementButton = (props: Props) => {
  const handleClick = () => {
    
  };

  return (
    <button
      onClick={handleClick}
      type="button"
      className="btn normal-case"
    >
      Start the Placement Test
    </button>
  );
};
